const User = require("./User");
const Thought = require("./Thought");
const Reaction = require("./Reaction"); // Ensure this is necessary if used elsewhere

module.exports = { User, Thought, Reaction };
